{
  "name": "syshelper-manager",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "cd ../web && pnpm run build",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute syshelper --file=schema.sql",
    "db:init:remote": "wrangler d1 execute syshelper --remote --file=schema.sql",
    "types": "wrangler types"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240529.0",
    "@types/bcryptjs": "^2.4.6",
    "typescript": "^5.4.5",
    "wrangler": "^3.57.0"
  }
}