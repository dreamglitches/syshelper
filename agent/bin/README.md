# These are placeholder files checked into git so //go:embed compiles.
# Run scripts/download-bins.sh to replace them with real static binaries
# before building. Real binaries are ignored by size (not tracked in git
# once they are non-empty).
#
# To force-add real binaries if needed: git add -f bin/upterm-amd64
